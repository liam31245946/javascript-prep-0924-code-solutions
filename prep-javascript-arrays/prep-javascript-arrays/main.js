const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];

const firstLanguage = languages[0];

const thirdLanguage = languages[2];

console.log('first element of the array is :', firstLanguage);
console.log('third element of the array is: ', thirdLanguage);

const languagesLength = languages.length;

console.log('the length of the array is ', languagesLength);

const lastLanguage = languages[languages.length - 1];

console.log('The last element in the array is:', lastLanguage);
